# =============================================================================
# Sub2API 测试环境配置模板
# =============================================================================
# 使用：
#   cp .env.test.example .env
#   # 生成独立密钥（务必与生产不同）：
#   echo "JWT_SECRET=$(openssl rand -hex 32)" >> .env
#   echo "TOTP_ENCRYPTION_KEY=$(openssl rand -hex 32)" >> .env
# =============================================================================

# -----------------------------------------------------------------------------
# Server —— 端口务必避开生产（生产 8080，测试用 8081）
# -----------------------------------------------------------------------------
BIND_HOST=0.0.0.0
SERVER_PORT=8081
# 测试环境建议 debug，方便看详细日志；压测/验收时可改回 release
SERVER_MODE=debug
RUN_MODE=standard
TZ=Asia/Shanghai

# -----------------------------------------------------------------------------
# PostgreSQL —— 密码必须与生产不同
# -----------------------------------------------------------------------------
POSTGRES_USER=sub2api
POSTGRES_PASSWORD=change_this_test_password
POSTGRES_DB=sub2api

# -----------------------------------------------------------------------------
# Redis
# -----------------------------------------------------------------------------
REDIS_PASSWORD=
REDIS_DB=0

# -----------------------------------------------------------------------------
# Admin 账号（首次启动自动创建）
# -----------------------------------------------------------------------------
ADMIN_EMAIL=admin@test.local
# 留空则首次启动自动生成，可在日志中查看：
#   docker compose -p sub2api-test logs sub2api | grep "admin password"
ADMIN_PASSWORD=

# -----------------------------------------------------------------------------
# 密钥 —— 必须与生产不同，建议用 openssl rand -hex 32 生成后填入
# -----------------------------------------------------------------------------
JWT_SECRET=
JWT_EXPIRE_HOUR=24
TOTP_ENCRYPTION_KEY=

# -----------------------------------------------------------------------------
# 其余可选项与生产一致，按需补充（参考 deploy/.env.example）
# -----------------------------------------------------------------------------
SECURITY_URL_ALLOWLIST_ENABLED=false
SECURITY_URL_ALLOWLIST_ALLOW_INSECURE_HTTP=true
SECURITY_URL_ALLOWLIST_ALLOW_PRIVATE_HOSTS=true
