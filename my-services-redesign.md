# 「我的服务」页面改造方案（替代原「API 密钥」页）

> 状态：待评审 · 最后更新：2026-06-08
> 适用范围：用户端 `/keys` 路由页面（面向终端用户，非管理员）

---

## 1. 背景与问题

当前用户端的核心页面是 `frontend/src/views/user/KeysView.vue`（「API 密钥」）。从产品视角看存在几个问题：

1. **术语太专业**："API 密钥""分组""倍率""速率限制"等词，普通用户看不懂。
2. **心智模型反了**：页面围绕"密钥（开发者凭证）"组织，而用户真正关心的是**"我有哪些 AI 服务、怎么付费、还能不能用、怎么接进我的工具"**。真正的产品单位——"服务（后端的分组 Group）"——被降级成了表格里一个小徽章。
3. **操作负担重**：创建密钥要填名称、分组、自定义 Key、IP 黑白名单、额度、5h/1d/7d 速率限制、有效期……对普通用户是劝退级复杂度。
4. **信息分散**：余额、订阅、可用渠道、密钥分散在多个页面，用户拼不出"我现在有什么"的全貌。
5. **计费方式不直观**：本站有两类计费——**按量付费（扣余额）** 与 **订阅套餐（包日/包周/包月）**，但当前 UI 用平台颜色区分，计费方式仅靠徽章深浅，用户最该一眼看清的维度反而被藏起来。

---

> **术语约定**：本文中的"**服务**"在系统里就等于"**分组（Group）**"。"**服务目录**"等价于**分组管理里能看到的全部分组**（即 `/admin/groups/all` 返回的那批数据）。用户端页面展示的就是这批分组，并按"是否可用"做可用/置灰处理。

## 2. 目标

### 2.1 产品目标
- 让用户以**最低认知成本**理解：①我有哪些 AI 服务 ②每个服务是什么计费方式 ③现在能不能用 ④怎么接入。
- **"密钥"概念对用户隐身**：用户不再需要理解、创建、命名、配置密钥。
- **零配置、傻瓜式接入**：能"一键开用"——自动装好客户端 App、配好所有环境变量。

### 2.2 体验目标（可度量）
- 接入一个服务的操作步骤：从原来的「创建密钥（多字段表单）→ 复制 → 看文档手动配置」压缩到 **点 1 个按钮**。
- 页面首屏信息量极简：每个服务仅展示「服务名 + 计费状态 + 价值换算 + 两个操作」。
- 用户能在 **3 秒内** 判断每个服务"能不能用"（可用/置灰一目了然）。

### 2.3 非目标（本期不做）
- 不重构 IA（余额/订阅/可用渠道页面暂不合并，仅在本页打通心智）。
- 不删除高级能力（额度/IP/有效期/多密钥管理）——保留为隐藏的"高级管理"入口。

---

## 3. 设计原则

1. **服务优先，密钥隐身**：页面叫"我的服务"，列的是服务，不是密钥。
2. **一屏一件事**：一个标题 + 余额药丸 + 一个极简列表，无概览大卡、无进度条、无多余统计。
3. **每行只留必要元素**：服务名、计费状态、价值换算小字、主按钮「一键开用」、次按钮「复制」。
4. **能不能用一眼可见**：不可用的服务整条置灰、按钮禁用，并在状态行说明原因。
5. **把抽象翻译成价值**：用"1 元 = 5 美元 token""每天可使用 50 美元 token"替代"倍率""limit"等术语。

---

## 4. 最终 UX 规格

### 4.1 页面结构
```
我的服务                                          [ 余额 $42.50 ]   (药丸仅展示，不可充值)
──────────────────────────────────────────────────────────────────
(Logo)  Claude                          [一键开用] [复制] [去充值]
        按量付费 · 余额 $42.50             帮你配置好所有相关应用与环境
        1 元 = 5 美元 token
──────────────────────────────────────────────────────────────────
(Logo)  Claude 套餐                      [一键开用] [复制] [去开通]
        ● 套餐 · 剩 12 天                  帮你配置好所有相关应用与环境
        每天可使用 50 美元 token
──────────────────────────────────────────────────────────────────
(灰) GPT                                 [一键开用禁用] [复制禁用] [去充值]
        按量付费 · 余额不足
        1 元 = 5 美元 token
──────────────────────────────────────────────────────────────────
(灰) Gemini 套餐                         [一键开用禁用] [复制禁用] [去开通]
        套餐 · 已到期
        每天可使用 50 美元 token
```

### 4.2 每行元素说明
| 元素 | 内容 | 备注 |
|---|---|---|
| 平台 Logo | Claude / GPT / Gemini / Antigravity | 复用 `PlatformIcon` |
| 服务名 | 分组名称 | 粗体主文字 |
| 小字第 1 行 | 计费状态（见下） | 稍深灰 |
| 小字第 2 行 | 价值换算（见下） | 更浅更小 |
| 主按钮 | **一键开用** | 蓝色实心；下方小字"帮你配置好所有相关应用与环境" |
| 次按钮 | **复制** | 描边/浅色 |
| 引导按钮 | **去充值**（按量）/ **去开通**（套餐） | 每行都有；置灰行此按钮仍可点；跳 `/purchase` 等 |

> 顶部余额药丸：仅展示余额，**不**作充值入口。

### 4.3 计费状态文案（小字第 1 行）
| 计费方式 | 可用 | 不可用 |
|---|---|---|
| 按量付费（standard） | `按量付费 · 余额 $42.50` | `按量付费 · 余额不足` |
| 套餐（subscription） | `● 套餐 · 剩 12 天` | `套餐 · 已到期` / `套餐 · 未开通` |

### 4.4 价值换算文案（小字第 2 行）
| 计费方式 | 文案 | 数据来源 |
|---|---|---|
| 按量付费 | `1 元 = 5 美元 token` | 由"充值汇率 × (1 / 分组倍率)"换算，见下方公式 |
| 套餐（日限额） | `每天可使用 50 美元 token` | `group.daily_limit_usd` |
| 套餐（周限额） | `每周可使用 X 美元 token` | `group.weekly_limit_usd` |
| 套餐（月限额） | `每月可使用 X 美元 token` | `group.monthly_limit_usd` |
| 套餐（无限额） | **不显示第 2 行（留空）** | 三个 limit 均为空时 |

**按量付费换算公式（已确认）**

- 站点充值汇率：**1 元 = 1 美元额度**（充值得到的是"美元额度/余额"）。
- 分组倍率：例如 **0.2x**，表示扣费时 1 美元额度可消耗 5 美元官方 token（`1 / 0.2 = 5`）。
- 因此：`每 1 元可用的美元 token = 充值汇率(美元/元) × (1 / 分组倍率)`，示例 `1 × (1 / 0.2) = 5` → "**1 元 = 5 美元 token**"。
- 若用户有**专属倍率**（`/groups/rates`），优先用专属倍率代入。
- 充值汇率取站点支付/汇率配置；倍率取 `group.rate_multiplier`（或用户专属）。文案随两者动态计算。

---

## 5. 可用性判定逻辑

- **按量付费分组**：`user.balance > 0` → 可用；否则置灰，状态"余额不足"。
- **套餐分组**：存在该 `group_id` 的订阅且 `剩余天数 > 0`（`expires_at` 推算）→ 可用，显示"剩 N 天"；
  - 有订阅但已过期 → 置灰，"已到期"；
  - 无该订阅（未购买）→ 置灰，"未开通"。
- 置灰行：「一键开用」「复制」均禁用，并以"去充值 / 去开通 / 去续费"CTA 取代/补充。

### 5.1 充值 / 开通引导（已确认：每一行都显示，无全局入口）

- **不设全局充值入口**：顶部余额药丸仅作余额展示（信息），不承担充值跳转。
- **每一行都显示引导 CTA**（无论可用还是置灰）：
  - **按量付费行**（可用 + 置灰）：显示"**去充值**" → 跳充值页。
  - **套餐行**（可用 + 置灰）：显示"**去开通**" → 跳购买/开通页（`/purchase`，带 `group` 参数）。
- 即每行操作区为：`一键开用` + `复制` + `去充值/去开通`（按计费方式二选一）。置灰行中 `一键开用`/`复制` 禁用，但 `去充值/去开通` 仍可点。
- 跳转目标：充值/购买复用现有路由（`/purchase` 等，落地时确认具体入口）。

---

## 6. 交互行为

### 6.1 一键开用（主操作）
1. 查找该服务（分组）下当前用户已有的可用凭证；没有则**懒创建**一把（`keysAPI.create(自动名称, group_id)`，用户无感）。
2. 用凭证拼接 `ccswitch://v1/import?...` deeplink（复用 `buildCcSwitchImportDeeplink`），由 CC-Switch 客户端自动装好/配好对应应用与环境变量。
3. `antigravity` 平台沿用现有"选择 Claude / Gemini 客户端"小弹窗。
4. 若未安装 CC-Switch，给出现有的"未安装"提示。

### 6.2 复制（次操作，给高级/手动用户）
1. 同样的"找/懒创建凭证"逻辑。
2. 复制凭证字符串到剪贴板，toast「已复制」。

### 6.3 凭证策略
- **一个服务复用一把凭证**：优先取该分组下已存在的 active 凭证；不存在才创建。
- 自动命名规则（建议）：`{服务名} 连接`，如"Claude 连接"。
- 不向用户暴露凭证字符串（除非点"复制"）。

---

## 7. 数据来源汇总

| 数据 | 来源 |
|---|---|
| 可用服务（用户有权用的分组） | `userGroupsAPI.getAvailable()`（含 `platform` / `subscription_type` / `rate_multiplier` / `daily/weekly/monthly_limit_usd`） |
| **服务目录（全部分组 = 分组管理所见）** | 概念等同 `/admin/groups/all`，但该接口是管理员专用；**需后端新增面向用户的只读接口**（见第 8.3 / 9 节，scope B），返回与分组管理相同的那批分组（仅可公开字段） |
| 余额 | `authStore.user.balance` 或 `userAPI.getProfile()` |
| 套餐剩余天数 / 状态 | `subscriptionsAPI.getMySubscriptions()`，按 `group_id` 匹配，`expires_at` 推算 |
| 已有凭证（按分组归集） | `keysAPI.list()` |
| 懒创建凭证 | `keysAPI.create(name, group_id)` |
| 一键配置 deeplink | `buildCcSwitchImportDeeplink()`（`utils/ccswitchImport.ts`） |
| 复制 | `useClipboard()` |
| 平台图标 | `PlatformIcon` |

---

## 8. 改动清单

### 8.1 新建
- `frontend/src/views/user/MyServicesView.vue` — 极简服务清单页，作为 `/keys` 的页面。
- `frontend/src/composables/useServiceCredential.ts` — 封装"按 group 找/懒创建 key + 复制 + 一键开用 deeplink"逻辑。

### 8.2 修改（前端）
- `frontend/src/router/index.ts` — `/keys` 指向 `MyServicesView`；旧 `KeysView` 挂到隐藏的高级入口路由（如 `/keys/advanced`）。
- `frontend/src/components/layout/AppSidebar.vue` — `nav.apiKeys` 文案改为"我的服务"，图标按需调整。
- `frontend/src/i18n/locales/zh.ts` / `en.ts` — 新增 `services.*` 文案（标题、计费状态、价值换算、一键开用及其副标题、余额不足、已到期、未开通等）；调整 `nav.apiKeys`。

### 8.3 修改（后端，仅 scope B 需要）
- "服务目录"在概念上 = 分组管理的全部分组（`/admin/groups/all`），但该接口仅管理员可用。因此**新增一个面向普通用户的只读接口**（如 `GET /groups/catalog`），返回**与分组管理相同的那批分组**（全部分组，而非仅用户有权用的）。
- 仅返回可公开展示字段：`id`、`name`、`platform`、`subscription_type`、`rate_multiplier`、`daily/weekly/monthly_limit_usd`、`sort_order`、`status` 等；**不返回**渠道/账号绑定、专属授权名单、内部备注等敏感信息。
- 可附带"当前用户对该分组是否可用 / 是否未开通"的标记，简化前端判断（也可由前端 join `getAvailable()` + 订阅 + 余额自行计算）。
- 前端数据组合：`服务目录(全部分组)` 为主列表 ⨝ `getAvailable()`(有权)＋`getMySubscriptions()`(套餐天数)＋`user.balance`(余额) → 算出每行可用/置灰与文案。

### 8.4 复用不改
`buildCcSwitchImportDeeplink` / `useClipboard` / `PlatformIcon` / `keysAPI` / `subscriptionsAPI` / `userGroupsAPI`。

---

## 8.5 关于"旧高级页"是什么

"旧高级页"就是**当前的 `frontend/src/views/user/KeysView.vue`（原「API 密钥」页）**，它提供了新极简页**刻意隐藏**的全部高级能力：

- 多密钥管理（一个服务可建多把密钥）、列表/搜索/筛选/分页/排序；
- 创建/编辑密钥的完整表单：名称、分组、**自定义 Key**、**IP 黑/白名单**、**额度上限 quota**、**速率限制（5h / 1d / 7d）**、**有效期**；
- 启用/停用、删除、重置额度、重置速率限制用量；
- 复制密钥、导入 CC-Switch、查看用量明细等。

这些是少数高级/团队用户才需要的能力。

**处理方式（已确认）：旧 `KeysView` 彻底不展示，不提供任何可见入口。**
- 新「我的服务」页**不放**"高级管理"链接，侧边栏也不暴露旧页。
- 代码与路由可暂时保留（仅供回退/内部排查），但用户在 UI 中无任何路径进入；后续可择机删除。
- 注意：旧页承载的高级能力（额度/IP/有效期/速率限制/多密钥）对普通用户即不可见——若将来仍需这些能力，须另行规划入口。

## 9. 已确认的决策

| 议题 | 决策 |
|---|---|
| 是否展示"未开通的套餐服务"（置灰） | ✅ **是（scope B）**——展示**全部分组（= 分组管理所见的服务目录）**含未开通；需后端新增面向用户的只读"服务目录"接口（数据等同 `/admin/groups/all`），本次一并改。 |
| 旧密钥页（含额度/IP/有效期等高级功能）如何处理 | ✅ **保留为隐藏的"高级管理"入口**（可回退，数据安全）。 |
| 主按钮命名 | ✅ **「一键开用」**，下方小字"帮你配置好所有相关应用与环境"。 |
| 「复制」点击行为 | ✅ 系统按服务懒创建并复用一把凭证，复制其字符串。 |
| 计费小字第 2 行 | ✅ 按量付费："1 元 = 5 美元 token"；套餐："每天/每周/每月可使用 X 美元 token"。 |
| 价值换算口径 | ✅ 站点充值 1 元=1 美元额度；按分组倍率换算（倍率 0.2x → 1 元=5 美元 token）；有专属倍率优先用专属。 |
| 套餐无限额第 2 行 | ✅ **留空**（不显示第 2 行）。 |
| 充值/开通引导 | ✅ **每一行都显示**"去充值"（按量）/"去开通"（套餐）；**不设**全局充值入口，顶部余额药丸仅展示。 |
| 旧高级页 | ✅ 即当前 `KeysView`（完整密钥管理）；**彻底不展示、不提供入口**（代码/路由可暂留以便回退）。 |

---

## 10. 待办 / 待确认

- [x] "1 元 = 5 美元 token" 取值口径 → 充值汇率 × (1/分组倍率)，专属倍率优先。
- [x] 套餐"无限额"时第 2 行文案 → 留空。
- [x] 充值/开通引导 → **每一行都显示**去充值（按量）/去开通（套餐）；不设全局入口，余额药丸仅展示。
- [x] 旧高级页 → 即现 `KeysView`；**彻底不展示、无入口**（代码/路由可暂留）。
- [x] 工作区恢复 → 已恢复至 `/Users/guozehang/sub2api`。

---

## 11. 分期与风险

**建议分期**
1. P0（无后端依赖）：新页 + 一键开用/复制 + 价值换算两行小字 + **每行去充值/去开通 CTA** + 文案；**旧 `KeysView` 不再展示**；先仅展示用户有权访问的服务。
2. P1（scope B）：后端面向用户的只读"服务目录"接口（= 全部分组）+ 前端展示未开通/已到期套餐（置灰 + 去开通/去续费）。
3. P2：IA 收敛（余额/订阅/可用渠道与本页心智打通）等优化。

**风险**
- CC-Switch 未安装时"一键开用"无法生效——已有兜底提示，但需保证"复制"作为后备路径清晰可达。
- 懒创建凭证可能在同一分组下产生多把 key（并发点击）——需做幂等/去重（优先复用已有 active key）。
- scope B 暴露"未开通"服务涉及后端权限边界，接口需只读且仅返回可公开展示字段。
